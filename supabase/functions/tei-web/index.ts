import "jsr:@supabase/functions-js/edge-runtime.d.ts";
const COMPANY_API="https://data.gcis.nat.gov.tw/od/data/api/5F64D864-61CB-4D0D-8AD9-492047CC1EA6";
const DIRECTOR_API="https://data.gcis.nat.gov.tw/od/data/api/4E5F7653-1B91-4DDC-99D5-468530FAE396";
const SUPABASE_URL=Deno.env.get("SUPABASE_URL")||"https://rztdbdurkjfrirsrrhtu.supabase.co";
const SERVICE_KEY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")||"";
const MOL="https://announcement.mol.gov.tw/cmpqry/";
const CORS={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"content-type,authorization,apikey","Access-Control-Allow-Methods":"GET,OPTIONS","Cache-Control":"no-store"};
const json=(x:unknown,status=200)=>new Response(JSON.stringify(x),{status,headers:{...CORS,"Content-Type":"application/json; charset=utf-8"}});
function arr(x:unknown):Record<string,unknown>[]{if(Array.isArray(x))return x.filter(v=>v&&typeof v==='object') as Record<string,unknown>[];if(x&&typeof x==='object'){const o=x as Record<string,unknown>;for(const k of ['data','records','items','result','value','rows']){const v=o[k];if(Array.isArray(v))return v.filter(z=>z&&typeof z==='object') as Record<string,unknown>[]}}return[]}
async function getJson(url:string,timeoutMs=9000){const c=new AbortController();const t=setTimeout(()=>c.abort(),timeoutMs);try{const r=await fetch(url,{signal:c.signal,headers:{"User-Agent":"T.E.I./5.8","Accept":"application/json"}});const text=await r.text();if(!r.ok)throw new Error(`HTTP ${r.status}: ${text.slice(0,180)}`);return JSON.parse(text) as unknown}finally{clearTimeout(t)}}
async function moea(api:string,u:string,top:number){const q=new URL(api);q.searchParams.set('$format','json');q.searchParams.set('$filter',`Business_Accounting_NO eq ${u}`);q.searchParams.set('$skip','0');q.searchParams.set('$top',String(top));return arr(await getJson(q.toString()))}
function pick(o:Record<string,unknown>,keys:string[]){for(const k of keys){const v=o[k];if(v!==undefined&&v!==null&&String(v).trim())return String(v).trim()}return''}
function host(v:string){try{return new URL(v.includes('://')?v:`https://${v}`).hostname.toLowerCase().replace(/^www\./,'').replace(/\.$/,'')}catch{return''}}
async function labor(name:string){const url=MOL+encodeURIComponent(name);const c=new AbortController(),t=setTimeout(()=>c.abort(),9000);try{const r=await fetch(url,{signal:c.signal,headers:{"User-Agent":"T.E.I./5.8"}});if(!r.ok)throw new Error(`MOL HTTP ${r.status}`);const text=await r.text();const rows:string[][]=[];for(const part of text.split('<tr').slice(1)){const row=part.split('</tr>')[0];if(!row.includes(name))continue;const cells:string[]=[];for(const cell of row.split('<td').slice(1)){const a=cell.indexOf('>'),z=cell.indexOf('</td>');if(a>=0&&z>a)cells.push(cell.slice(a+1,z).replace(/<[^>]*>/g,' ').replace(/&nbsp;/g,' ').replace(/ +/g,' ').trim())}if(cells.length)rows.push(cells)}return{status:'ok',matched:rows.length,records:rows.slice(0,30),source_url:url}}finally{clearTimeout(t)}}
async function upsert(path:string,row:unknown){if(!SERVICE_KEY)return{ok:false,status:0,error:'service key not configured'};const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{method:'POST',headers:{apikey:SERVICE_KEY,Authorization:`Bearer ${SERVICE_KEY}`,Accept:'application/json','Content-Type':'application/json',Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify(row)});const text=await r.text();return{ok:r.ok,status:r.status,error:r.ok?undefined:text.slice(0,600)}}
async function dbGet(path:string){if(!SERVICE_KEY)return[];const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{headers:{apikey:SERVICE_KEY,Authorization:`Bearer ${SERVICE_KEY}`,Accept:'application/json'}});return r.ok?await r.json():[]}
async function persist(uniform:string,c:Record<string,unknown>,people:Record<string,string>[]){if(!SERVICE_KEY)return{status:'not_configured'};const now=new Date().toISOString();const company={uniform_number:uniform,name:pick(c,['Company_Name','Juristic_Person_Name','Business_Name'])||uniform,status:pick(c,['Company_Status','Status','Company_Status_Desc']),representative_name:pick(c,['Responsible_Name','Representative_Name','President_Name']),address:pick(c,['Company_Location','Company_Address','Address']),capital_amount:c['Capital_Amount']??c['Capital_Stock_Amount']??null,established_date:pick(c,['Company_Setup_Date','Establishment_Date']),source:'MOEA/GCI',source_url:COMPANY_API,fetched_at:now,raw_ref:`moea:${uniform}`};const a=await upsert('companies',[company]);if(!a.ok)return{status:'error',stage:'companies',error:a.error};const cr=await dbGet(`companies?select=id&uniform_number=eq.${uniform}&limit=1`);const companyId=Array.isArray(cr)?cr[0]?.id:null;if(!companyId)return{status:'error',stage:'company_id'};let peopleWrites=0,edgeWrites=0,evidenceWrites=0,failures:string[]=[];for(let i=0;i<people.length;i++){const p=people[i];const pn=await upsert('people',[{name:p.person_name,source:'MOEA/GCI',fetched_at:now}]);if(!pn.ok){failures.push(`people:${i}:${pn.error}`);continue}peopleWrites++;const pr=await dbGet(`people?select=id&name=eq.${encodeURIComponent(p.person_name)}&limit=1`);const pid=Array.isArray(pr)?pr[0]?.id:null;if(pid){const ep=await upsert('company_people',[{company_id:companyId,person_id:pid,role:p.position,source:'MOEA/GCI',source_url:DIRECTOR_API,fetched_at:now}]);if(ep.ok)edgeWrites++;else failures.push(`company_people:${i}:${ep.error}`)}else failures.push(`person_id:${i}:${p.person_name}`);const ev=await upsert('evidence',[{entity_type:'company',entity_key:uniform,source_type:'moea_director',title:`${p.position}：${p.person_name}`,summary:'官方公司登記董監事資料中的職務關係。',source_url:DIRECTOR_API,external_id:`${uniform}:director:${i}:${p.person_name}`,fact_type:'director_relationship',observed_at:now,matched_terms:[p.person_name,p.position],confidence:1,raw_ref:`moea:director:${uniform}:${i}`}]);if(ev.ok)evidenceWrites++;else failures.push(`evidence:${i}:${ev.error}`)}const ec=await upsert('evidence',[{entity_type:'company',entity_key:uniform,source_type:'moea_company',title:company.name,summary:'官方公司登記基本資料快照。',source_url:COMPANY_API,external_id:`${uniform}:company`,event_date:company.established_date||null,fact_type:'company_registration',observed_at:now,matched_terms:[company.name,uniform],confidence:1,raw_ref:`moea:company:${uniform}`}]);if(ec.ok)evidenceWrites++;else failures.push(`company_evidence:${ec.error}`);return failures.length?{status:'partial',people_writes:peopleWrites,edge_writes:edgeWrites,evidence_writes:evidenceWrites,failures}:{status:'ok',people_writes:peopleWrites,edge_writes:edgeWrites,evidence_writes:evidenceWrites}}
async function local(uniform:string){if(!SERVICE_KEY)return{configured:false,evidence_count:0,evidence:[]};const e=await dbGet(`evidence?select=*&entity_type=eq.company&entity_key=eq.${encodeURIComponent(uniform)}&limit=150`);return{configured:true,evidence_count:Array.isArray(e)?e.length:0,evidence:Array.isArray(e)?e:[]}}
function domainHits(domain:string,e:Record<string,unknown>[]){if(!domain)return[];return e.filter(x=>[x.source_url,x.title,x.summary].some(v=>{const h=host(String(v??''));return !!h&&(h===domain||h.endsWith(`.${domain}`)||domain.endsWith(`.${h}`))}))}
const WEBSITE:Record<string,string>={'23060248':'https://www.family.com.tw'};
async function investigate(u:string){if(!/^\d{8}$/.test(u))return{status:'error',error:'統編必須為 8 碼數字'};const [cr,dr]=await Promise.all([moea(COMPANY_API,u,1),moea(DIRECTOR_API,u,1000)]);if(!cr.length)return{status:'not_found',uniform_number:u};const c=cr[0],name=pick(c,['Company_Name','Juristic_Person_Name','Business_Name'])||u;const people=dr.map(r=>({person_name:pick(r,['Person_Name','person_name','Name']),position:pick(r,['Person_Position_Name','position','Position'])||'董監事'})).filter(x=>x.person_name);const site=pick(c,['Company_Website','Website','website','WebSite','網址','網站'])||WEBSITE[u]||'';const before=await local(u);const persistence=await persist(u,c,people);const after=await local(u);let l:any;try{l=await labor(name)}catch(e){l={status:'error',matched:0,records:[],error:String(e)}}const domain=host(site);const hits=domainHits(domain,after.evidence);const nodes=[{id:'company',type:'company',label:name},...people.slice(0,80).map((p,i)=>({id:`p${i}`,type:'person',label:p.person_name}))];const edges=people.slice(0,80).map((p,i)=>({source:'company',target:`p${i}`,relationship:p.position}));return{status:'ok',uniform_number:u,company:c,company_name:name,people,graph:{nodes,edges},website:site?{url:site,host:domain}:null,domain,labor:l,anti_fraud:{status:domain?'ok':'no_website',matched:hits.length,records:hits.slice(0,20),match_rule:'cached evidence only; exact domain/subdomain'},local_context:after,persistence:{...persistence,before_evidence_count:before.evidence_count,after_evidence_count:after.evidence_count},evidence_status:{'公司登記':{status:'ok',matched:1},'董監事':{status:'ok',matched:people.length},'勞動裁罰':l,'公司網址×165':{status:domain?'ok':'no_website',matched:hits.length},'我的資料':{status:'ok',matched:after.evidence_count},'司法院':{status:'link',matched:0}}}}
const HTML=String.raw`<!doctype html><html lang="zh-Hant"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>T.E.I.</title><style>:root{--bg:#070a0f;--panel:#0d131b;--line:#26313e;--text:#edf2f7;--muted:#8b98a8;--blue:#7fb0ff;--green:#63d39f;--amber:#f2c56a;--red:#ef7d8d}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:system-ui,-apple-system,"Noto Sans TC",sans-serif}.top{height:64px;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;padding:0 24px}.brand{font-weight:800}.sub{font-size:9px;color:#718094;letter-spacing:1.3px}.badge{color:var(--green);font-size:10px}.search{padding:14px 24px;border-bottom:1px solid var(--line);display:flex;gap:10px}.search input{flex:1;max-width:900px;height:44px;border:1px solid #334151;border-radius:8px;background:#111821;color:var(--text);padding:0 14px;font-size:16px}.search button{height:44px;border:1px solid #7597c4;border-radius:8px;background:#dfeafb;color:#0e1620;padding:0 18px;font-weight:800}.grid{display:grid;grid-template-columns:280px minmax(0,1fr) 390px;height:calc(100vh - 122px)}aside{overflow:auto;background:var(--panel)}.left{border-right:1px solid var(--line)}.right{border-left:1px solid var(--line);padding:14px}.sec{padding:14px;border-bottom:1px solid var(--line)}.muted{color:var(--muted);font-size:10px}.title{font-size:20px;font-weight:800;margin:8px 0}.src{display:flex;justify-content:space-between;gap:8px;border:1px solid var(--line);border-radius:8px;padding:9px;margin:7px 0;background:#0c1117;font-size:10px}.ok{color:var(--green)}.warn{color:var(--amber)}.bad{color:var(--red)}.graph{position:relative;overflow:hidden;background:radial-gradient(circle at 50% 45%,#121b26 0,#0a0f15 44%,#070a0f 82%)}svg{position:absolute;inset:0;width:100%;height:100%}.edge{stroke:#536173;stroke-width:1.3;opacity:.7}.node text{fill:#fff;stroke:#070a0f;stroke-width:4;paint-order:stroke;font-size:14px;font-weight:600}.node.center text{font-size:15px;font-weight:800}.node circle{stroke:#070a0f;stroke-width:2}.card{border:1px solid var(--line);border-radius:8px;background:#0c1117;padding:10px;margin-bottom:9px}.card h3{font-size:13px;margin:0 0 8px}.record{border-top:1px solid #273241;padding:8px 0;font-size:9px;line-height:1.45}.record:first-child{border-top:0}.notice{border:1px solid #2d3947;border-radius:8px;background:#0b1118;padding:10px;color:#8b98a8;font-size:9px;line-height:1.6}@media(max-width:1050px){.grid{grid-template-columns:250px 1fr}.right{display:none}}@media(max-width:720px){.grid{grid-template-columns:1fr}.left{display:none}}</style></head><body><div class="top"><div><div class="brand">Taiwan Entity Intelligence</div><div class="sub">PUBLIC-RECORD INVESTIGATION WORKSPACE · 5.8</div></div><div class="badge">● LIVE SOURCES</div></div><div class="search"><input id="q" maxlength="8" value="23060248"><button id="go">開始調查</button></div><div class="grid"><aside class="left"><div class="sec"><div class="muted">TARGET ENTITY</div><div id="name" class="title">尚未查詢</div><div id="uid" class="muted">—</div><div id="stats"></div></div><div class="sec"><div class="muted">SOURCE STATUS</div><div id="sources"></div></div></aside><section class="graph"><svg id="svg" viewBox="0 0 1000 620" preserveAspectRatio="xMidYMid meet"></svg><div id="empty" class="notice" style="position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);text-align:center">輸入統編後顯示企業關係圖</div></section><aside class="right"><div id="detail"><div class="notice">查詢結果會顯示在這裡。</div></div></aside></div><script>const q=document.getElementById('q'),b=document.getElementById('go');const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));function source(k,v){const st=v?.status||'error',cls=st==='ok'?'ok':(st==='link'||st==='no_website'?'warn':'bad'),tx=st==='ok'?String(Number(v.matched||0).toLocaleString())+' 筆':st==='link'?'官方查詢入口':st==='no_website'?'無公司網址':'查詢失敗';return '<div class="src"><span>'+esc(k)+'</span><b class="'+cls+'">'+esc(tx)+'</b></div>'}function graph(g){const s=document.getElementById('svg');s.innerHTML='';const ns=g.nodes||[],es=g.edges||[];if(!ns.length)return;const pos=new Map([[ns[0].id,{x:500,y:310}]]),other=ns.slice(1),r=Math.min(250,115+other.length*7);other.forEach((n,i)=>{const a=-Math.PI/2+i/Math.max(1,other.length)*Math.PI*2;pos.set(n.id,{x:500+Math.cos(a)*r,y:310+Math.sin(a)*r})});const layer=document.createElementNS('http://www.w3.org/2000/svg','g');s.appendChild(layer);es.forEach(e=>{const a=pos.get(e.source),d=pos.get(e.target);if(!a||!d)return;const l=document.createElementNS('http://www.w3.org/2000/svg','line');l.setAttribute('class','edge');l.setAttribute('x1',a.x);l.setAttribute('y1',a.y);l.setAttribute('x2',d.x);l.setAttribute('y2',d.y);layer.appendChild(l)});ns.forEach((n,i)=>{const p=pos.get(n.id),g1=document.createElementNS('http://www.w3.org/2000/svg','g');g1.setAttribute('class','node '+(i===0?'center':''));const c=document.createElementNS('http://www.w3.org/2000/svg','circle');c.setAttribute('cx',p.x);c.setAttribute('cy',p.y);c.setAttribute('r',i===0?36:19);c.setAttribute('fill',i===0?'#7fb0ff':'#63d39f');g1.appendChild(c);const t=document.createElementNS('http://www.w3.org/2000/svg','text');t.textContent=n.label;t.setAttribute('x',p.x);t.setAttribute('y',p.y+(i===0?58:38));t.setAttribute('text-anchor','middle');g1.appendChild(t);layer.appendChild(g1)});document.getElementById('empty').style.display='none'}function render(x){document.getElementById('name').textContent=x.company_name||'查無公司';document.getElementById('uid').textContent=x.uniform_number||'—';document.getElementById('stats').innerHTML='<div class="src"><span>董監事</span><b>'+((x.people||[]).length)+'</b></div><div class="src"><span>關係邊</span><b>'+((x.graph?.edges||[]).length)+'</b></div><div class="src"><span>Evidence</span><b>'+Number(x.local_context?.evidence_count||0).toLocaleString()+'</b></div>';const ss=x.evidence_status||{};document.getElementById('sources').innerHTML=Object.keys(ss).map(k=>source(k,ss[k])).join('');const p=x.persistence||{},l=x.labor||{},f=x.anti_fraud||{};let h='<div class="card"><h3>'+esc(x.company_name)+'</h3><div class="record">統編：'+esc(x.uniform_number)+'</div>'+(x.website?'<div class="record">公司網址：<a href="'+esc(x.website.url)+'" target="_blank" style="color:#91baff">'+esc(x.website.url)+'</a></div>':'')+'</div><div class="card"><h3>資料寫入狀態</h3><div class="record">companies / people / graph / evidence</div><div class="record"><b class="'+(p.status==='ok'?'ok':p.status==='partial'?'warn':'bad')+'">'+esc(p.status||'unknown')+'</b> · evidence '+Number(p.before_evidence_count||0)+' → '+Number(p.after_evidence_count||0)+'</div></div><div class="card"><h3>勞動法令裁罰 · '+Number(l.matched||0).toLocaleString()+' 筆</h3>';(l.records||[]).slice(0,8).forEach(r=>{h+='<div class="record">'+esc(Array.isArray(r)?r.join(' · '):r)+'</div>'});if(!(l.records||[]).length)h+='<div class="notice">來源無命中或目前無法讀取。</div>';h+='</div><div class="card"><h3>公司網址 × 165</h3><div class="record">比對網域：'+esc(x.domain||'—')+'</div><div class="record">結果：<b class="'+(f.matched?'bad':'ok')+'">'+(f.matched?'命中 '+f.matched+' 筆':'目前未命中')+'</b></div></div><div class="notice">公開紀錄僅表示存在資料，不直接構成犯罪或法律結論。</div>';document.getElementById('detail').innerHTML=h;graph(x.graph||{})}async function run(){const id=q.value.trim();if(!/^\d{8}$/.test(id)){document.getElementById('name').textContent='統編需為 8 碼數字';return}b.disabled=true;b.textContent='查詢中…';try{const r=await fetch('?api=company&uniform='+encodeURIComponent(id),{cache:'no-store'}),x=await r.json();if(!r.ok||x.status!=='ok')throw new Error(x.error||'查詢失敗');render(x)}catch(e){document.getElementById('name').textContent='查詢失敗';document.getElementById('uid').textContent=e.message}finally{b.disabled=false;b.textContent='開始調查'}}b.onclick=run;q.onkeydown=e=>e.key==='Enter'&&run();run();</script></body></html>`;
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response(null,{status:204,headers:CORS});const u=new URL(req.url);try{if(u.searchParams.get('api')==='company')return json(await investigate(u.searchParams.get('uniform')||''));if(u.pathname.endsWith('/health'))return json({status:'ok',service:'tei-web',version:'5.8',persist:true});return new Response(HTML,{headers:{...CORS,'Content-Type':'text/html; charset=utf-8'}})}catch(e){return json({status:'error',error:String(e)},500)}});